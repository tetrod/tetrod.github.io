name: Website Build
on:
  push:
    branches:
      - main
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2.3.1

      - name: Install
        run: npm i

      - name: Build 🔧
        run: npm run build

      - name: Deploy pages 🚀
        run: npm run deploy
